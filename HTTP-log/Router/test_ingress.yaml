echo "
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: http-log-ingress-osp
  annotations:
    konghq.com/plugins: http-log-plugin-osp
spec:
  ingressClassName: kong
  rules:
  - http:
      paths:
      - path: /http_osp
        pathType: ImplementationSpecific
        backend:
          service:
            name: echo
            port:
              number: 80
" | kubectl apply -f - -n kong